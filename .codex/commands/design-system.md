---
name: design-system
description: Run the design-system skill for EasyQuest tokens, component APIs, and reusable React Native UI planning.
argument-hint: <tokens, component family, or UI area>
---

# /design-system

Use `design-system` for this EasyQuest task:

`$ARGUMENTS`

Plan only the MVP-sized design system pieces needed for tasks, submissions, approval actions, points, rewards, and child wishes. Do not create code components unless explicitly requested.

Return the result in this format:

- Token decisions
- Component list
- Props API
- TypeScript interfaces
- Folder structure
- Implementation steps
- What not to build yet
